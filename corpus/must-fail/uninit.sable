// expect-error: type.uninitialized
// x is only initialized on one branch (design §2.3: no default zero).

/// post result >= 0
fn pick_bad(bool b) -> i32 {
    mut i32 x;
    if (b) { x = 1; }
    return x;
}
