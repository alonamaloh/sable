// Helper module: two exported functions, used by the collision and
// unknown-name guards.

/// pre  x < 100
/// post result = x + 1
pub fn bump(i32 x) -> i32 {
    return x + 1;
}

/// post result = x
pub fn same(i32 x) -> i32 {
    return x;
}
