// expect-error: type.recursion_needs_variant
// Recursion without a decreasing measure. (With `/// variant n` this
// verifies — see corpus/verifies/gcd.sable.)

/// post result = 0
fn countdown(u32 n) -> u32 {
    if (n == 0) { return 0; }
    return countdown(n - 1);
}
