// expect-error: type.member_param
// An option member parameter is refused even though ordinary functions
// admit value-payload option parameters: the member call boundary has not
// been given the transport.

pub class C {
    u64 v;

    init make(option<u64> o) {
        self.v = 0;
    }
}
