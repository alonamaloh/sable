// expect-error: type.bool_array_field
// Boolean-array field ownership and lowering remain deferred.

class Flags {
    [bool] values;

    init new() {
        self.values = alloc_array<bool>(1, false);
    }
}
