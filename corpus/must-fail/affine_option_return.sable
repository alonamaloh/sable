// expect-error: type.affine_option_return
// An affine option owns its payload, and no return transports one: it is
// constructed, inspected, and emptied where it is declared.

fn return_option() -> option<[bool]> {
    return none;
}
