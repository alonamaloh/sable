// expect-error: type.affine_option_param
// An affine option owns its payload, and no call boundary transports one:
// it is constructed, inspected, and emptied where it is declared.

fn inspect_parameter(option<[bool]> pending) -> bool {
    return pending.is_some;
}
