// expect-error: type.affine_option_field
// Conditional ownership in aggregate storage needs a field layout and
// destructor rule; G2.1 keeps affine options as locals.

class Holder {
    option<[bool]> pending;

    init empty() {}
}
