// The simplest loop: invariant + variant, havoc, and the after-loop
// facts (invariant ∧ ¬condition) proving the post.

/// post result = n
fn count_up(u64 n) -> u64 {
    mut u64 i = 0;
    /// invariant i ≤ n
    /// variant   n - i
    while (i < n) {
        i = i + 1;
    }
    return i;
}
